name: AI Social Media Automation Scheduler
name: Scheduled AI Social Media Agent

on:
  schedule:
    # Runs 3 times daily (UTC):
    # 03:00 UTC = 09:00 AM BST (Bangladesh Standard Time)
    # 09:00 UTC = 03:00 PM BST
    # 15:00 UTC = 09:00 PM BST
    - cron: '0 3,9,15 * * *'
workflow_dispatch: # Allows manual trigger from GitHub UI
  schedule:
    # Runs 3 times daily at 08:00, 14:00, and 20:00 UTC
    - cron: '0 8,14,20 * * *'

jobs:
  run-agent:
  run-automation:
runs-on: ubuntu-latest

steps:
- name: Checkout Repository
uses: actions/checkout@v4

      - name: Setup Python
      - name: Set up Python
uses: actions/setup-python@v5
with:
python-version: '3.11'
@@ -27,11 +24,12 @@ jobs:
         python -m pip install --upgrade pip
         pip install -r requirements.txt

      - name: Execute Social Media Agent
      - name: Execute Content Automation Script
env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          IMGBB_API_KEY: ${{ secrets.IMGBB_API_KEY }}
FB_PAGE_ID: ${{ secrets.FB_PAGE_ID }}
          FB_PAGE_ACCESS_TOKEN: ${{ secrets.FB_PAGE_ACCESS_TOKEN }}
IG_USER_ID: ${{ secrets.IG_USER_ID }}
        run: python agent.py
          FB_PAGE_ACCESS_TOKEN: ${{ secrets.FB_PAGE_ACCESS_TOKEN }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GSPREAD_CREDS_JSON: ${{ secrets.GSPREAD_CREDS_JSON }}
        run: |
          python agent.py
